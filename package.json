{
  "name": "demis",
  "version": "1.0.0",
  "description": "DEMIS — Anthony's stateful agentic assistant",
  "private": true,
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "test":   "node tests/suite.js",
    "dev":    "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@vercel/kv":        "^3.0.0"
  }
}
